# 开发指引

实现 ECMAScript 能力、选择下一切片或调整标准范围时，先读取 [.agents/ecmascript-plan.md](.agents/ecmascript-plan.md)。

## 纵向切片

- **定义切片**：每次选择一项可执行的 JS 能力，先明确源码输入及预期结果或错误。
- **端到端实现**：首个 MVP 打通 CLI 输入源码、执行并输出结果的链路；后续切片在这条链路上扩展，各层只实现当前切片所需的逻辑。
- **按需设计**：模块划分服务于切片实现；GC、JIT 等基础设施随可验证的功能或性能需求引入。
- **完成标准**：新增能力及其错误路径的端到端测试、已有回归测试全部通过，CLI 保持可用，再进入下一切片。

## 引擎实现参考

- **语义、模块划分、字节码执行**：实现或调整这些部分前，查阅 [Boa](https://github.com/boa-dev/boa) 的对应实现。
- **值表示、堆、GC**：设计或调整这些部分前，查阅 [Nova](https://github.com/trynova/nova)，重点参考其与 Rust 类型和生命周期的结合方式。
- **对象访问、热点优化、JIT、退优化**：设计或优化这些机制前，查阅 [V8](https://v8.dev/docs)，重点参考机制间的协作。
